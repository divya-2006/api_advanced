[project]
name = "api-advanced"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "divya-2006", email = "divyachittala82934@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]

[tool.vercel]
entrypoint = "src.api_advanced.main:app"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
